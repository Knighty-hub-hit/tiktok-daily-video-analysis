name: Daily data refresh (legacy disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Explain disabled workflow
        run: |
          echo "This legacy GitHub Actions refresh workflow is disabled."
          echo "Current production refresh runs from the local Codex automation and publishes the internal Feishu Miaoda site."
          echo "No TikTok export, Feishu write, GitHub Pages deploy, or Feishu group notification is performed here."
